[workspace]

[package]
license = "MIT-0"
name = "no-src-locations"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
er = { path = "../../er", default-features = false, features = ["macros"] }

[dev-dependencies]
trybuild = "1.0"
